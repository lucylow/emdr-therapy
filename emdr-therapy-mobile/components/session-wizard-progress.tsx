import { StyleSheet, Text, View } from 'react-native';
import { DS } from '@/components/ui/design-system';

const phases = ['Baseline', 'Target', 'Processing', 'Closure'];
export function SessionWizardProgress({ active }: { active: number }) { return <View style={styles.wrap}>{phases.map((phase, index) => <View key={phase} style={styles.item}><View style={[styles.circle, index <= active && styles.circleActive]}><Text style={[styles.number, index <= active && styles.numberActive]}>{index + 1}</Text></View><Text style={[styles.label, index === active && styles.labelActive]}>{phase}</Text>{index < phases.length - 1 ? <View style={[styles.line, index < active && styles.lineActive]} /> : null}</View>)}</View>; }
const styles = StyleSheet.create({ wrap: { flexDirection: 'row', alignItems: 'flex-start', justifyContent: 'space-between', marginVertical: 8 }, item: { flex: 1, alignItems: 'center', position: 'relative' }, circle: { width: 28, height: 28, borderRadius: 14, borderWidth: 1, borderColor: DS.line, backgroundColor: DS.surface, alignItems: 'center', justifyContent: 'center', zIndex: 2 }, circleActive: { backgroundColor: DS.deep, borderColor: DS.deep }, number: { color: DS.muted, fontSize: 11, fontWeight: '700' }, numberActive: { color: '#FFFFFF' }, label: { color: DS.muted, fontSize: 9, marginTop: 6 }, labelActive: { color: DS.ink, fontWeight: '800' }, line: { position: 'absolute', top: 14, left: '50%', right: '-50%', height: 1, backgroundColor: DS.line }, lineActive: { backgroundColor: DS.deep } });
